mutation DeleteUserMutation($input: DeleteUserQuery!) {
  deleteUser(input: $input) {
    name
    id
    currentPunch
    status
    history {
      id
      userId
      entry
      leave
      status
    }
  }
}
