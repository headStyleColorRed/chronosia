query AllUsersQuery {
	allUsers {
        name
        id
        currentPunch
        history {
            id
            userId
            entry
            leave
            status
        }
  }
}
