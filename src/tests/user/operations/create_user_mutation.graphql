mutation CreateUserMutation($input: CreateUserInput!) {
  createUser(input: $input) {
    name
    id
    currentPunch
    status
    history {
      id
      userId
      entry
      leave
      status
    }
  }
}
