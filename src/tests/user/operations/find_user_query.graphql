query FindUserQuery($input: FindUserQuery!) {
  userWithId(input: $input) {
    name
    id
    currentPunch
    status
    history {
      id
      userId
      entry
      leave
      status
    }
  }
}
